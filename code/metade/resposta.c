#include "llist.h"
#include <stdlib.h>

LList *metade(LList *l) {
  // Escreva uma função que devolve o elemento do meio de uma lista ligada
  // Se tiver tamanho par, devolve o elemento à direita da metade.
  // 
  // DICA: pode ser útil criar outras funções auxiliares
  // DICA2: tente andar de dois em dois nós
  
  return NULL;
}
